alias l='ls -al'

# adds git aliases
alias gs='git status '
alias gss='git status -s '
alias ga='git add '
alias gb='git branch '
alias gc='git commit '
alias gd='git diff '
alias co='git checkout '
alias gk='gitk --all&'
alias gx='gitx --all'
alias gh='git hist '
alias gist='git hist '
alias gitup='git submodule foreach git pull origin master'

# postgres
alias pgstatus='pg_ctl status -D /db/postgres'
alias pgstart='pg_ctl -D /db/postgres -l /db/postgres/server.log start'
alias pgstop='pg_ctl -D /db/postgres -l /db/postgres/server.log stop'
alias blendpgstart='pg_ctl -o "-p 5433" -D /db/blend -l /db/postgres/server.log start'
alias blendpgstop='pg_ctl -o "-p 5433" -D /db/blend -l /db/postgres/server.log stop'
alias pglist='ps -ef | grep postgres'

# make ipython run inside virtualenv when possible
alias ipython="python -c 'import IPython; IPython.terminal.ipapp.launch_new_instance()'"
# python virtualenvwrapper aliases
alias ve.workon='workon'
alias ve.deactivate='deactivate'
alias ve.mk='mkvirtualenv'
alias ve.rm='rmvirtualenv'
alias ve.switch='workon'
alias ve.add2virtualenv='add2virtualenv'
alias ve.cdsitepackages='cdsitepackages'
alias ve.cd='cdvirtualenv'
alias ve.ls='lssitepackages'

# python pyc remove
alias cleanpyc='find . -name "*.pyc" -print0 | xargs -0 -n1 rm'

# directory traversal
alias ....='cd ../../../'

# search for process
alias ps.find='ps -ef | grep'

# make and cd into directory
function mcd () {
  mkdir -p "$1" && cd "$1";
}

alias warntap='screen -D -R -S warntap ssh -t shells01.ec2.hipmunk.com warntap.sh'
alias warntappy='screen -D -R -S warntappy ssh -t shells01.ec2.hipmunk.com warntappy.sh'
alias warntapjs='screen -D -R -S warntapjs ssh -t shells01.ec2.hipmunk.com warntapjs.sh'
alias haproxystatus='screen -D -R -S deploy_status ~/Code/Work/hipmanage/hipmanage/haproxystatus.py'

alias flush.redis='echo "FLUSHALL" | redis-cli'
alias flush.mc='printf "flush_all\nquit\n\0" | nc localhost 11211'
alias flush.all='printf "flush_all\nquit\n\0" | nc localhost 11211 && echo "FLUSHALL" | redis-cli'
alias gyp="~/Code/Work/hipmanage/hipmanage/git.py"
alias gyp.clone="~/Code/Work/hipmanage/hipmanage/git.py clone"
alias gyp.push="~/Code/Work/hipmanage/hipmanage/git.py push"
alias gyp.commit="~/Code/Work/hipmanage/hipmanage/git.py commit"
# update hipmange
alias gyp.update="git -C ~/Code/Work/hipmanage pull --rebase upstream master"
