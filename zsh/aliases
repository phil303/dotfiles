# tmux
alias tmux='TERM=screen-256color-bce tmux'

# adds git aliases
alias gs='git status '
alias gss='git status -s '
alias ga='git add '
alias gb='git branch '
alias gc='git commit '
alias gd='git diff '
alias go='git checkout '
alias gk='gitk --all&'
alias gx='gitx --all'
alias gh='git hist '
alias gist='git hist '
alias gitup='git submodule update'

alias got='git '
alias get='git '

# Use to find rails server accidentally left running
alias rl='lsof -wni tcp:3000'

# postgres
alias pgstatus='pg_ctl status -D /db/postgres'
alias pgstart='pg_ctl -D /db/postgres -l /db/postgres/server.log start'
alias pgstop='pg_ctl -D /db/postgres -l /db/postgres/server.log stop'
alias pglist='ps -ef | grep postgres'

# python virtualenvwrapper aliases
alias ve.workon='workon'
alias ve.deactivate='deactivate'
alias ve.mk='mkvirtualenv'
alias ve.rm='rmvirtualenv'
alias ve.switch='workon'
alias ve.add2virtualenv='add2virtualenv'
alias ve.cdsitepackages='cdsitepackages'
alias ve.cd='cdvirtualenv'
alias ve.ls='lssitepackages' 

# python pyc remove
alias cleanpyc='find . -name "*.pyc" -print0 | xargs -0 -n1 rm'

# directory traversal
alias ....='cd ../../../'

# search for process
alias ps.find='ps -ef | grep'

# make and cd into directory
function mcd () {
  mkdir -p "$1" && cd "$1";
}

# extract most known archive types
extract () {
  if [ -f $1 ] ; then
    case $1 in
      *.tar.bz2)   tar xjf $1     ;;
      *.tar.gz)    tar xzf $1     ;;
      *.bz2)       bunzip2 $1     ;;
      *.rar)       unrar e $1     ;;
      *.gz)        gunzip $1      ;;
      *.tar)       tar xf $1      ;;
      *.tbz2)      tar xjf $1     ;;
      *.tgz)       tar xzf $1     ;;
      *.zip)       unzip $1       ;;
      *.Z)         uncompress $1  ;;
      *.7z)        7z x $1        ;;
      *)     echo "'$1' cannot be extracted via extract()" ;;
      esac
    else
      echo "'$1' is not a valid file"
    fi
}

alias flush.redis='echo "FLUSHALL" | redis-cli'
alias flush.mc='printf "flush_all\nquit\n\0" | nc localhost 11211'
alias flush.all='printf "flush_all\nquit\n\0" | nc localhost 11211 && echo "FLUSHALL" | redis-cli'
